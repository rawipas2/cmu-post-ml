# Thai Depression Classification - Requirements
# 
# Python Version Required: 3.10
#
# Installation Guide:
# 
# Option 1 (Recommended): Using Conda
# ----------------------------------
# conda create -n thai-depression python=3.10
# conda activate thai-depression
# conda install -c rapidsai -c conda-forge -c nvidia cuml cupy
# conda install pytorch torchvision pytorch-cuda=11.8 -c pytorch -c nvidia
# pip install -r requirements.txt
#
# Option 2: Using pip only
# ------------------------
# python -m venv venv
# .\venv\Scripts\Activate.ps1  (Windows) or source venv/bin/activate (Linux/Mac)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# pip install -r requirements.txt
# pip install cupy-cuda11x  (may have compatibility issues)
#
# Quick Setup (Windows):
# -----------
# 1. Run: python check_python_version.py  (to verify Python 3.10)
# 2. Run: .\setup.ps1
#
# Linux/Mac: bash setup.sh

# Core ML/DL Frameworks (ติดตั้งแยกตามคำแนะนำด้านบน)
# torch>=2.0.0
# torchvision>=0.15.0

# GPU Acceleration - RAPIDS AI (ติดตั้งผ่าน conda แนะนำ)
# cuml-cu11>=23.0.0
# cupy-cuda11x>=12.0.0

# ติดตั้งผ่าน pip
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
scipy>=1.11.0

# Thai NLP
pythainlp>=3.1.0
attacut>=1.1.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
